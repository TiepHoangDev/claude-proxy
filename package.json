{
  "name": "claude-proxy",
  "private": true,
  "scripts": {
    "build": "go build -o build/proxy.exe ./cmd/proxy",
    "start": "go run ./cmd/proxy",
    "vet": "go vet ./...",
    "clean": "rm -rf build"
  }
}
